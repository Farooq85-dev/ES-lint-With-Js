// ESLint is a tool for identifying and reporting on patterns found in ECMAScript/JavaScript code, with the goal of making code more consistent and avoiding bugs.

// Dont allow to insert log
console.log(hello());

// Throw errors to use semi colon
const semi = "Semi";

// Throw error to use triple equals
if (semi == "Semi") {
  console.log("true");
}

// Thorow error of umused variables
const hello = "Farooq";

// Thorow error of not defined variable
console.log(vars);

// Thorow error of curly brackets
if (true) console.log("Runing...");

// Thorow error of single quotes
console.log("Hello Superman!");


let a = "Muhammad Farooq";